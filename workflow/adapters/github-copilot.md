# GitHub Copilot adapter

Read `workflow/spec-driven-development.yaml` and `specifications/` before
working. Infer the relevant stage from the task and announce it. For a
deterministic CI or human entry point, use `member-assistant-specs select-stage`.
This file is only an adapter; portable specifications remain authoritative.
