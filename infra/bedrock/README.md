# Amazon Bedrock Configuration

This directory contains Amazon Bedrock agent and model configuration for Nexus.

## Components
- Model: `anthropic.claude-3-sonnet` (default)
- Agent: `conversation_agent` (defined in `/agents/conversation_agent.yaml`)

## Deployment
Configure the Bedrock agent using the AWS Console or CDK/Terraform definitions in this directory.
