# Nexus development-agent adapter

Use `workflow/skills/nexus-spec-driven-development/SKILL.md` for platform,
contract, connector, capability, test, and release work. Read
`specifications/constitution.md`, infer and announce the active workflow stage,
then follow the corresponding portable stage skill.

This file is a thin Claude Code adapter. The portable specifications and
workflow skills are the source of truth. Never edit an immutable published
catalog skill; develop a traced candidate in its capability package.
